<% provide(:title, 'All Packages') %>
<h1>All Packages</h1>

<%= will_paginate %>

<ul class="packages">
  <% @packages.each do |package| %>
    <li>
      <%= link_to package.name, package %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>

